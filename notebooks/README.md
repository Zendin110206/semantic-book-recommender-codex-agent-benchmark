# Notebook Scope

`data-exploration.ipynb` is kept as the exploration notebook from the learning phase.

The source of truth for the portfolio project is now the Python package under `src/semantic_book_recommender/`. Build steps, metadata generation, semantic indexing, and the app should be run from the scripts in the repository root.
